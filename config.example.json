{
  "database": {
    "url": "postgres://$DB_USER:$DB_PASS@$DB_HOST:$DB_PORT/$DB_NAME?sslmode=disable"
  },
  "hash": {
    "bcrypt": "$BCRYPT_HASH"
  },
  "server": {
    "http": {
      "port": "$SERVER_PORT",
      "version": "$SERVER_VERSION"
    }
  }
}